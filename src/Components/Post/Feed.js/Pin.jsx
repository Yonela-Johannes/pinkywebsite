import React from 'react'
import { urlFor } from '../../../client'

export default function Pin({pin}) {
    console.log(pin)
  return (
    <div>
        {/* <img className='feedImage' alt="manager post" src={urlFor(image).width(320).url()} /> */}
    </div>
  )
}
